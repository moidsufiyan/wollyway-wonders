// Legacy Vite entry — not used in Next.js App Router.
// Keep as a no-op module to avoid build-time type errors while the repo
// contains both the old React Vite app and the Next.js app.

export {};
